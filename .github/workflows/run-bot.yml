name: "Run Security Bot"

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  call-bot:
    uses: your-username/your-repo/.github/workflows/repo-security-bot.yml@main
    secrets:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
